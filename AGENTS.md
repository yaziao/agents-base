# 通用 AI 开发规范

本文件定义稳定、通用的 AI 开发规则，不绑定具体语言、框架、平台或架构。项目专属规则可以在此基础上逐步补充，但不应破坏这些基本边界。

## 三层职责

- AGENTS.md：定义 AI 的基本工作原则、修改边界、安全边界、验证要求和长任务规则。
- .agents/skills/：提供可按需加载的具体开发流程。Skill 不是所有任务都必须执行的总流程。
- .ai/：记录当前项目的任务状态和长期有效的重要决策。

## 工作原则

默认工作顺序是：

~~~text
理解 → 定位 → 最小修改 → 验证
~~~

1. 先理解用户目标、当前代码和已有约束。
2. 再定位真正需要修改的位置和根因。
3. 只做解决当前任务所必需的最小修改。
4. 用与风险相称的真实检查验证结果。

如果在工作中发现任务范围或风险发生变化，应说明变化并重新评估，而不是默默扩大修改。

## 修改范围

- 不修改与当前目标无关的代码、配置、文档或格式。
- 不擅自进行架构重构、目录迁移、命名体系调整或大范围风格清理。
- 不随意增加、替换或升级依赖；新增依赖前先检查现有能力和必要性。
- 优先复用项目已有的实现、约定和工具。
- 如果额外修改确实是解决当前问题所必需的，应说明原因、范围和风险。

## 任务边界

用户要求修复 Bug 时，优先完成 Bug 修复，不自动扩大为：

- Bug 修复加架构重构
- Bug 修复加依赖升级
- Bug 修复加代码风格调整
- Bug 修复加无关功能

只有当这些额外变化是解决问题、保证安全或完成验证不可避免的一部分时，才纳入当前任务。

## 用户修改保护

- 开始工作前检查工作区和相关文件的现状。
- 将用户已有的未提交修改视为需要保护的内容。
- 不覆盖、回退或清理用户修改，除非用户明确要求且目标范围已确认。
- 不未经允许执行危险的 Git 操作或不可逆的数据操作。
- 如果现有修改与当前任务冲突，先说明冲突、影响和可选处理方式。

## 调试

- 优先复现问题并收集证据。
- 优先定位根因，而不是通过猜测不断试改。
- 检查调用路径、状态变化、输入输出和错误传播。
- 修复应针对已确认的原因，并避免把临时绕过方案伪装成根治。
- 如果无法复现或证据不足，应明确说明不确定性和下一步验证方式。

## 验证

- 验证应尽量接近真实使用路径，并覆盖改动的主要风险。
- 根据项目已有能力选择合适的测试、构建、静态检查、运行检查或人工检查。
- 验证失败时记录失败现象、原因和未完成事项，不要掩盖失败。
- 没有执行真实验证，不得声称“测试通过”“构建成功”“已修复”或“已验证”。
- 最终说明做过哪些检查，以及哪些检查因环境或范围限制未执行。

## 安全

对所有外部输入和边界保持警惕，重点关注：

- 输入校验、输出编码和错误信息
- 身份、权限和访问控制
- 敏感信息的读取、存储、日志和传输
- 命令执行、脚本调用和注入风险
- 文件路径、文件权限和路径穿越
- 网络请求、重定向、证书和外部服务边界
- 反序列化、模板、查询或其他解释执行边界

不要为了方便把安全检查删除、降级或改成依赖调用方自觉的约定。

## 性能

- 不进行无依据的性能优化。
- 先通过实际代码路径、数据规模、测量结果或测试确认瓶颈。
- 优先选择局部、可验证、可回退的优化。
- 不用复杂度和可维护性换取未经证实的收益。

## 依赖和外部变化

- 新增依赖前检查项目中是否已有可复用能力。
- 评估依赖的维护状态、许可、供应链和引入范围。
- 不因为顺手而升级无关依赖。
- 外部接口或环境发生变化时，明确记录假设和验证结果。

## 长任务和上下文

长任务使用以下文件维护可恢复状态：

- .ai/current-task.md：记录当前目标、进度、约束、验证和下一步。
- .ai/decisions.md：记录长期有效的重要技术决策及其理由。

任务较复杂、耗时较长、跨多个阶段或可能经历上下文压缩时，应及时更新状态。上下文压缩后优先恢复这些文件、当前工作区和已确认的决策。

不要因为上下文压缩就推翻已经确认的技术决策；只有出现新的证据、约束或明确需求时才重新评估。

## Skills

Skills 按需使用：

- 简单修改可以直接完成，不必执行复杂流程。
- 普通 Bug 通常需要系统调试和完成前验证。
- 新功能或复杂改动可以按需要组合设计、计划、执行、测试和 Review。
- 大型任务可以加入上下文管理、影响分析、并行 Agent 或子 Agent。

不要要求所有任务执行完整的 Superpowers 流程，也不要因为某个 Skill 存在就扩大任务范围。Skill 提供能力，当前任务的风险和复杂度决定是否使用。

## 沟通和完成

- 在修改前明确目标、范围和关键假设。
- 发现范围变化、风险或阻塞时及时说明。
- 最终报告修改内容、真实验证结果、已知问题和后续建议。
- 把推测、未验证结果和已完成结果清楚区分。
